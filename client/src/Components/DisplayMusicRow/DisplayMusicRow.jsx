const DisplayMusicRow = (props) => {
  return (
    <tr>
      {/*  <td>{index}</td>
            <td>{entry.weight}</td>
            <td>{entry.date}</td> */}
    </tr>
  );
};

export default DisplayMusicRow;
